{
  "name": "minimal-weakref-addon",
  "scripts": {
    "firefox": "web-ext run -s extension --verbose --firefox nightly  --no-config-discovery",
    "chrome": "web-ext run -s extension --verbose --target chromium  --no-config-discovery"
  },
  "devDependencies": {
    "web-ext": "^6.6.0"
  }
}
